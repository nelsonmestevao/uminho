{erl_opts, [debug_info]}.

{deps, []}.

{project_plugins, [erlfmt]}.

{erlfmt, [
    write,
    {files, ["{src,include,test}/*.{hrl,erl,app.src}", "rebar.config"]}
]}.

{shell, [
    % {config, "config/sys.config"},
    {apps, [login_manager]}
]}.
